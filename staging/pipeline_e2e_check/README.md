# pipeline_e2e_check

Throwaway rapplication used to verify the `kody-w/RAPP_Store` submission pipeline end-to-end (issue → process workflow → staging → approve → promote → catalog).

Not intended for installation. Will be reverted from the catalog after the test run.
